const qs=(s)=>document.querySelector(s);const qsa=(s)=>document.querySelectorAll(s);qsa('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=qs(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'});}}));
const menu=qs('.menu');const nav=qs('.nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));qsa('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.08});qsa('.section,.work-card,.job,.cap,.principles>div').forEach(e=>io.observe(e));
qsa('.job').forEach(j=>j.addEventListener('click',()=>{qsa('.job').forEach(x=>x.classList.remove('active'));j.classList.add('active')}));
window.addEventListener('scroll',()=>{const y=scrollY;qs('.hero-visual')?.style.setProperty('transform',`translateY(${Math.min(y*.06,35)}px)`);},{passive:true});